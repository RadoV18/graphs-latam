export const setSourceNode = (data) => {
    return {
        type: "SET_SOURCE_NODE",
        data
    }
}

export const setTargetNode = (data) => {
    return {
        type: "SET_TARGET_NODE",
        data
    }
}
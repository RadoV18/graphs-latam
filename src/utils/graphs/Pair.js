export default class Pair {
    /**
     * @param first distance from source
     * @param second vertex number/name
     */
    constructor(first, second) {
        this.first = first;
        this.second = second;
    }
}